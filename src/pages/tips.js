import React from 'react';
import { Layout } from '../components/Layout';

const TipsPage = ({ pageContext }) => {
	return <Layout {...pageContext}>Tips, prompt engineering</Layout>;
};

export default TipsPage;
